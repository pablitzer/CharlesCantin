{% set page = general.logo.linkedpage %}
<div class="nav-brand col-6 col-lg-4 d-flex justify-content-end p-0"
<a href="{{pages[page].url }}" title="{{general.logo.hovertext}}" ><img src="{{general.logo.image}}" alt="logo Charles Cantin"></a>
</div>